<html>
<body>
  <%= render "shared/header" %>

  <div class="container col-8">
    
      <% if user_signed_in? %>
        <p class="text-center"><%= "#{current_user.last_name_kana} #{current_user.first_name_kana}"%></p>
        <h2 class="text-center font-weight-bold"><%= "#{current_user.last_name} #{current_user.first_name} 様邸"%> </h2>
        <% if @tasks.present? %>
          <div class="shadow p-3 mb-5 bg-white rounded d-flex justify-content-center">
            <div>ただいまのお手入れタスクの数は</div>
            <div><%= @task.count %></div>
            <div>件です</div>
          </div><br>
        <%else%>
          <div class="shadow p-3 mb-5 bg-white rounded d-flex justify-content-center">
            <div>登録されているタスクはありません</div>
          </div>
        <%end%>
      <% else %>
        <h2 class="text-center font-weight-bold"><%= "ゲストユーザー様" %> </h2>
        <br>
        <p class="lead text-center"><%="本日は貴重なお時間を頂き誠にありがとうございます。"%></p><br>
      <% end %><br><br>
    
      
    <div class="jumbotron shadow p-3 mb-5 bg-white rounded">
      <div class="d-flex justify-content-center">
      <h3 class="font-weight-bold">お手入れタスク一覧</h3>
      </div>

      <div class="d-flex flex-row-reverse">
          <%= link_to '全て', tasks_path(:show=>'all'), class: 'btn btn-primary btn-sm ml-2'%>
          <%= link_to '未完了', tasks_path, class: 'btn btn-primary btn-sm ml-1'%><br>
      </div>
    
    <div class="container flex-column align-items-center">
      <% @tasks.each do |task| %>
      <tr id="task-#{task.id}">
      <div class="card text-center" data="<%= task.id %>">
        <div class="card-header">
          <td><%=task.floor%></td>
          <td><%= task.room%></td>
        </div>

        <div class="card-body">
          <td><h5 class="card-title"><%=task.item%></h5></td>
          <td><p class=><%=task.mentenance%></p></td>
          <td>
            <%= link_to '詳細画面へ', task_path(task), class:"btn btn-primary btn-sm" %><br><br>
            <%= link_to '削除する', task, method: :delete,remote: true, data:{ confirm: "タスクを削除します。よろしいですか？", task_id: task.id}, class: 'btn btn-danger' %>
          </td>
        </div>
        
        <div class="card-footer font-weight-bold">
          <%=deadline_count(task)%>
        </div>
      </div>
      <br>
      </tr>

      <% end %>
    </div>
    
    <%= paginate @tasks%>
      <!-- Optional JavaScript -->
      <!-- jQuery first, then Popper.js, then Bootstrap JS -->
      <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    </div>
  </div>

  <div class="row d-flex justify-content-center">
    <div>
      <a class="btn btn-lg" href="/tasks/new" role="button">⏩早速お手入情報の登録はこちらから⏪</a>
    </div><br>
  </div>
  
    <%= render "shared/footer" %>
  </body>
</html>