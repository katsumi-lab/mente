<html>
<body>
  <%= render "shared/header" %>

  <div class="container col-8">
    <div class="jumbotron">
      <% if user_signed_in? %>
        <p class="text-center"><%= "#{current_user.last_name_kana} #{current_user.first_name_kana}"%></p>
        <h2 class="text-center font-weight-bold"><%= "#{current_user.last_name} #{current_user.first_name} 様邸"%> </h2>
        <br>
        <p class="lead"><%="お住まいになられて◯年△ヶ月経ちました。快適にお過ごしでしょうか？長く快適に暮らす為には日頃のお手入れが非常に重要となります。忘れそうなお掃除はお手入れ登録しましょう！" %></p>
      <% else %>
        <h2 class="text-center font-weight-bold"><%= "ゲストユーザー様" %> </h2>
        <br>
        <p class="lead text-center"><%="本日は貴重なお時間を頂き誠にありがとうございます。"%></p>
      <% end %><br><br>
    
    <div class="row d-flex justify-content-between">
      <div>
        <p>早速お手入れタスク情報を登録しましょう</p>
      </div>
      <div>
        <a class="btn btn-primary btn-md" href="/tasks/new" role="button">新規登録</a>
      </div>
    </div>
  </div> 

    <div class="jumbotron">
      <div class="d-flex justify-content-center">
      <h2 class="font-weight-bold">お手入れタスク一覧</h2>
      </div>

      <div class="d-flex flex-row-reverse">
        <% if @task%>
          <%= link_to '全て', tasks_path(:show=>'all'), class: 'btn btn-primary btn-sm ml-2'%>
          <%= link_to '未完了', tasks_path, class: 'btn btn-primary btn-sm ml-1'%>
        <% else %>
          <%= link_to '全て', tasks_path(:show=>'all'), class: 'btn btn-outline-primary btn-sm ml-2'%>
          <%= link_to '未完了', tasks_path, class: 'btn btn-primary btn-sm ml-1'%>
        <%end%></br>
      </div>
    
    <div class="container flex-column align-items-center">
      <% @tasks.each do |task| %>
      <div class="card text-center">
        <div class="card-header">
          <%=task.floor%>
          <%= task.room%>
        </div>
        <div class="card-body">
          <h5 class="card-title"><%=task.item%><%=task.mentenance%></h5>
          <p class="card-text"><%=task.description%></p>
          <%= link_to '詳細画面へ', task_path(task), class:"btn btn-primary btn-sm" %>
        </div>
        <div class="card-footer text-muted">
          <%=deadline_count(task)%>
        </div>
      </div>
      <br>
      <% end %>
    </div>
    
    <%= paginate @tasks%>
      <!-- Optional JavaScript -->
      <!-- jQuery first, then Popper.js, then Bootstrap JS -->
      <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    </div>
  </div>

    <%= render "shared/footer" %>
  </body>
</html>